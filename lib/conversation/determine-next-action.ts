// Phase 3: maps (conversationStage, missing fields, riskLevel, intent) to the
// next allowed action / conversation stage (spec section 9).
// TODO: implement as an explicit state machine, not a free-form LLM decision.
